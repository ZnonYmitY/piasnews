# Iteration 1 review status

These are author-drafted golden candidates for human review. They are not independent model runs and must not be reported as benchmark results.

After the candidate rules are reviewed and the source set reaches the release gate, run blinded comparisons across base, RAG-only, style-only, judgment-only, and full-skill configurations.
